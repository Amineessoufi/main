import React from 'react';
import './header.css';

const Header = () => {
  return (
    <header className="padding-5">
      {/* Slider controls, header content */}
    </header>
  );
};

export default Header;
